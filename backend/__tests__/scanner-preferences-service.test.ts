// test file intentionally left blank; duplex preference is handled by UI tests which are not
// executed in the backend test suite

describe('ScannerPreferencesService', () => {
  it('placeholder test to satisfy Jest 30+ requirement', () => {
    expect(true).toBe(true);
  });
});
