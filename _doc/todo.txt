GarbageFinder:
- add delete action
- add sort by size toggle option on top bar nexz to reset button
Commander:
- at startup no ftp and no sambe restore, always start with local folders (last used), save history to settings
- in ftp dir up => go to dir where we came from
- dir size dialog cannot be cancelled
- loading directory... should show whole path
- Ctrl+a should select all files
- ctrl+pageup/down: select all files between these
- add examples for replace in texteditor (e.g. line feeds)
- new dos2unix and unix2dos actions in text editor (show current format bottom right)
- copy from zip (or from zip in zip) doesnt work or is very slow
- exceptions in read dirs should not block app
later:
- add support for browse into asar files
- cleanup ipc side to really use only functions from help output in command line, enhance commandline and its help to use all backend functions
- test and fix ftps
- generate tests for all backend features
other:
- build app for linux
- db tool like bweaver
- quick launcher ctrl+Space (global shortcut)
- quick text inserter (global shortcut)
- integrate more cmdline-Tools
  - versioning (git simple)
  - dirsorter for jpegs
  - mp3metatagsetter for mp3s
  - imdb finder
    - find imdb rating quickly by typing film name 
- music player (IPOD-like see www.nilshendricks.de>widgets)
- pdf manager
  - split pdf
  - get text from pdf (ocr)
  - find text in pdfs 
    - write text for each pdf
    - find text
  - rename pdf (date/sender/account)
