Prompts and costs

1. add command in backend to list files in a folder, copy or move a file from one location to antoher (0,60)
2. fix ui service call Unknow operation (0,60)
3. Send screenshot and d:/temp (erfolglos 0,80)
4. If i dont explicitely choose a combo box entry in ui the value is '' (0,70)

5. Implementiere in meine neue Seite Commander.ts einen File-Commander wie den Total Commander. 
   Operatioinen anzeigen, Verzeichnisse wechseln (klicken oder ENTER), Datei anschauen (F3), 
   Dateien markieren (Strg + click bzw. Strg und Pfeil hoch und runter) sollen mehrere Dateien auswählen. 
   Dann kopieren mit F5 oder verschieben mit F6.
   (0,53 !!!)
6. Die Fenster sollen nebeneinander sein, der commander genau in den verfügbaren Bereich passen. Mit Enter oder Doppelklick soll in das Verzeichnis gewechselt werden, das gerade aktiv ist. Mit Pfeil rauf und runter soll man in einem Verzeichnis links und rechts (Tab wechselt den Fokus von links nach rechts und umgekehrt) die aktuell gewählte Datei ändern können. 
   (0,85 hä?)
7. Die Fenster sollen links und rechts angezeigt werden.
   In der Höhe soll der Commander den verfügbaren Platz ,maximal ausnutzen.
   Ich sehe ich beiden Fenster d:\, kann aber nicht hineinnavigieren. weder mit ENTER noch mit Klick. Das musst Du fixen.
   (0,30)
8. Fehlermeldung aus Konsole reinkopert
   (0,40)

9. Die Fenster sind noch übereinander. Sie sollen nebeneineander sein. Eines links, eines rechts.
   (0,09)

10. Im Kopieren Dialog wird ENTER nicht erkannt. Ich will auch ganze Verzeichnisse kopieren / verschieben können.   
  (0,30)

11. Die Navigation klappt nicht von d:/devtools über .. nach oben. Er landet im aktuellen Verzeichnis der Anwendung. Außerdem will ich, dass beim click auf das aktuelle Verzeichnis (header) eine Auswahl der verfügbaren Verzeichnisse kommt. Dafür brauchst Du vermutlich noch einen neuen command.
2.18